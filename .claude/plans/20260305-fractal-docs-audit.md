# [DeepRead 分形文档与自动回环审计计划]

根据工程规范中对“分形文档体系”的定义，进行全面的自底向上/自顶向下的审查。

- [x] L1 (全局地图)：检查根目录的 `CLAUDE.md` 完整性与映射性。 (已存在并涵盖全局要素)
- [x] L2 (模块清单)：检查核心模块（`src/`, `tests/`, `src/prompts/` 等）中是否存在 `CLAUDE.md` 并更新同步状态。 (确认 src, tests, src/prompts 目录下均有对应的 CLAUDE.md 摘要文件)
- [x] L3 (文件头契约)：遍历项目中所有 `.py` 文件，验证头部是否严格包含 `[IN]/[OUT]/[POS]/[PROTOCOL]` 四要素。 (通过脚本扫描发现所有 17 个 .py 文件这 4 个标签完整无缺)
- [x] 针对 L2/L3 的缺漏进行修补与同步，确保与各模块逻辑功能契合。 (由于无破窗效应，此项自然完成)
